// Props ->1.  maxWidth : This is the max container width,
// if(imageWidth >= maxWidth)
// {
//     imageContainer = maxWidth;
// }
// else {
//         imageContainer = imageWidth
// }
// ->2. Static prop takes in boolean. If it is true the image is static and if it is not it is false.
